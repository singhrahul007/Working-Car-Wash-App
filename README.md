# Frontend (Expo) - CarWash
## Setup
1. cd frontend
2. npm install
3. npx expo start

## Notes
- Configure EXPO_PUBLIC_API_BASE_URL environment variable for API calls.
- Uses react-native-paper for UI.
